import { Route, Routes } from "react-router-dom";  import { Navbar } from "@/components/navbar"; import { Sidebar } from "@/components/sidebar"; import ProtectedRoute from "@/components/ProtectedRoute"; import LoginPage from "@/pages/auth/login"; import RegisterPage from "@/pages/auth/register"; import AdminDashboard from "@/pages/admin/dashboard"; import AdminUsers from "@/pages/admin/users"; import BookingsPage from "@/pages/user/bookings"; import CalendarPage from "@/pages/user/calendar"; import DashboardPage from "@/pages/user/dashboard"; import FacilitiesPage from "@/pages/user/facilities"; import HelpPage from "@/pages/user/help"; import NotificationsPage from "@/pages/user/notifications"; import ProfilePage from "@/pages/user/profile";  function App() {   return (     <div className="relative flex flex-col min-h-screen bg-background text-foreground transition-colors duration-300">       {/* Background Pattern */}       <div         className="fixed inset-0 z-[-1] opacity-[0.03] dark:opacity-[0.05] pointer-events-none"         style={{           backgroundImage:             "radial-gradient(circle at 2px 2px, currentColor 1px, transparent 0)",           backgroundSize: "32px 32px",         }}       />        <div className="flex flex-col lg:flex-row min-h-screen w-full">         <Sidebar />         <div className="flex flex-col flex-1 w-full">           <Navbar />           <main className="container mx-auto max-w-7xl pt-12 px-6 flex-grow pb-20">             <Routes>               <Route element={<LoginPage />} path="/login" />               <Route element={<RegisterPage />} path="/register" />                {/* Protected Routes */}               <Route element={<ProtectedRoute />}>                 <Route element={<FacilitiesPage />} path="/facilities" />                 <Route element={<BookingsPage />} path="/bookings" />                 <Route element={<CalendarPage />} path="/calendar" />                 <Route element={<NotificationsPage />} path="/notifications" />                 <Route element={<HelpPage />} path="/help" />                 <Route element={<ProfilePage />} path="/profile" />               </Route>                {/* User Only Routes */}               <Route element={<ProtectedRoute userOnly />}>                 <Route element={<DashboardPage />} path="/" />               </Route>                {/* Admin Only Routes */}               <Route element={<ProtectedRoute adminOnly />}>                 <Route element={<AdminDashboard />} path="/admin" />                 <Route element={<AdminUsers />} path="/admin/users" />               </Route>             </Routes>           </main>           <footer className="w-full flex items-center justify-center py-6 border-t border-default-100 bg-background/50 backdrop-blur-sm">             <div className="flex flex-col items-center gap-2">               <p className="text-default-400 text-xs font-medium">                 Γö¼ΓîÉ 2026 RoomSync Booking System               </p>               <div className="flex gap-4 text-[10px] text-default-300">                 <span>Privacy Policy</span>                 <span>Terms of Service</span>                 <span>Contact Support</span>               </div>             </div>           </footer>         </div>       </div>     </div>   ); }  export default App;